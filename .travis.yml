
language: php

php:

 - 7.0
 - 7.1
 - 7.2
 - 7.3

before_script:
  - mysql -e 'CREATE DATABASE test;'

services:
 - mysql
 
script:
  script: bash scripts/deploy.sh
 
 deploy:
  skip-cleanup: true
  # Publish docs
  provider: script
  script: npm run test:deploy-results
  on:
    branch: build
  # Test reports
  provider: script
  script: npm run test:deploy-results
  on:
    branch: build
 
notifications:
 email: your_email